<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.laila.unifacs.atividadepraticaunidade_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AtividadePraticaUnidade_1">
        <activity
            android:name=".activity.LockScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog"
            android:autoRemoveFromRecents="true"/>

        <activity
            android:name=".activity.ConfigScreenActivity"
            android:exported="true"
            android:label="@string/tela_config" />
        <activity
            android:name=".activity.MainScreenActivity"
            android:exported="true"
            android:label="@string/tela_principal" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:label="@string/tela_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>